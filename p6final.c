#include<stdio.h>
void input_string(char *a)
{
  printf("Enter a string\n");
  scanf("%s",a);
}
int count_words(char *string)
{
  
}
void output(char *string, int no_words)
{
  printf("the number of words in string %s is %d",);
}
int main()
{
  int k,no_words;
  string string;
  input_string(&a);
  k=count_words(&string);
  output(&string,no_words);
  return 0;
}
